# 当前工作区

每个独立功能或修复使用一个 Markdown 文件。文件名采用简短英文 `kebab-case`，完成后移入 `archive/`。

建议模板：

```markdown
# 任务名称

## 背景
为什么要做。

## 目标
最终可观察到的结果。

## 范围
- 包含：
- 不包含：

## 实现决定
- 决定及原因。

## 验收标准
- [ ] 用户可观察的行为
- [ ] 异常或边界行为
- [ ] 验证方式

## 影响文件
- `path/to/file`
```

不要在这里存放密钥、账号、用户隐私数据或完整运行日志。
